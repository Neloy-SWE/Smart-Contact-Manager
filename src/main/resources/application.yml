spring:
  application:
    name: smart_contact_manager

#  servlet:
#    multipart:
#      enabled: true
#      max-file-size: 300MB
#      max-request-size: 300MB
#      file-size-threshold: 1MB

  datasource:
    url: jdbc:mysql://localhost:3306/smartcontact
    username: root
    password: mysql@neloy
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

    hibernate:
      ddl-auto: update

    show-sql: true

server:
  tomcat:
    max-swallow-size: -1